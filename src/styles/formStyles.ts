import { StyleSheet } from 'react-native';

export const formStyles = StyleSheet.create({
  field: {
    borderWidth: 1,
    borderColor: '#ccc',
    borderRadius: 10,
    paddingVertical: 14,
    paddingHorizontal: 14,
    marginVertical: 8,
    backgroundColor: '#fff',
  },
  placeholder: {
    color: '#999',
  },
  text: {
    color: '#000',
  },
});
